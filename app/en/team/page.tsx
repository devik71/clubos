import { FirstTask } from '../../../components/first-task';
import { PageIntro, SiteFooter, SiteHeader } from '../../../components/site-shell';
import { TeamWorkspace } from '../../../components/team-workspace';

export const metadata = { title: 'People — Building together' };
export default function EnglishTeamPage() {
  return <><SiteHeader current="team" locale="en" /><main><PageIntro label="04 / THE TEAM JOINS THE DEVELOPMENT" title={<>Your experience.<br /><span className="muted">Your tools.</span></>} text="Kseniia knows how light should feel. Mykola knows which matters need attention. The bar manager knows what must be ready before opening. That expertise is the starting point for development." /><section className="wrap route-demo"><TeamWorkspace locale="en" /></section><section className="section wrap" id="start-together"><div className="split-heading"><h2 className="hub-h2">Enter the system<br /><span className="muted">through your own work.</span></h2><p>You do not need to know the entire stack at once. Take a familiar problem, define one small change, work with an agent, and review the result with a technical lead.</p></div><div className="learning-steps">{[['Define it', 'What is awkward now? What should change? How will we see it?'], ['Understand the plan', 'The agent explains a small step. You refine the idea and the success criterion.'], ['Try it', 'Make the change locally. Check the normal case and one case where something goes wrong.'], ['Share it', 'Show the team and document the rule. Next: a separate Git branch, review, and a Vercel preview.']].map(([title, text], i) => <div key={title}><span>0{i + 1}</span><h3>{title}</h3><p>{text}</p></div>)}</div><FirstTask locale="en" /></section><section className="hub-closing dark-section"><div className="wrap"><span className="eyebrow">SHARED PRINCIPLE</span><h2>The agent shortens the path.<br /><span className="muted">People set the direction.</span></h2><p>A technical lead stewards the architecture and helps others learn. The team judges usefulness in real work. Every clear module becomes shared experience.</p></div></section></main><SiteFooter locale="en" /></>;
}
